import React from "react";

import { Maintenance, ScreenContainer } from "components";

const Saved = () => {
  return (
    <ScreenContainer>
      <Maintenance />
    </ScreenContainer>
  );
};

export default Saved;
